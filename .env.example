# Server-side only. Used by /api/po-intake/parse for live PDF extraction.
# Never rename this with a NEXT_PUBLIC_ prefix and never commit a real value.
ANTHROPIC_API_KEY=

# Optional canonical URL override for metadata in non-production environments.
NEXT_PUBLIC_SITE_URL=
